# To get started with Dependabot version updates, you'll need to specify which
# package ecosystems to update and where the package manifests are located.
# Please see the documentation for all configuration options:
# https://docs.github.com/code-security/dependabot/dependabot-version-updates/configuration-options-for-the-dependabot.yml-file

version: 2
updates:
  - package-ecosystem: "gomod" # See documentation for possible values
    directory: "/" # Location of package manifests
    schedule:
      interval: "weekly"

  # The workflows pin every action by commit SHA, which dependabot understands
  # and rewrites (SHA + trailing version comment) on each bump. Without this
  # entry those pins never move: checkout and setup-go had drifted a full major
  # behind before it was added.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
